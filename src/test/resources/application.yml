spring:
  jooq:
    sql-dialect: postgres
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/app_db?createDatabaseIfNotExist=true
    username: root
    password: password